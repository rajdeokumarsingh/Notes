--debug=v
    显示make的详细流程

--just-print (-n)
    显示make的工作流程，但是并不执行命令

--print-data-base (-p)
    输出所有的隐藏规则和内部变量

--directory=dir-name (-C dir-name)
    到指定目录中执行make

--file (-f)
    指定makefile文件。但是不能进入makefile文件的目录

--silent (-s)
    不现实执行的命令

--ignore-errors (-i)

--keep-going (-k)
