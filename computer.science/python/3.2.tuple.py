元组

基本概念 {
    const类型的列表

    x = () # 空元组

    x = (1) # FIXME: x只是一个值为1的变量
    x = (1,) # 一个元素的元组，需要添加逗号
    x = 1,   # 一个元素的元组，需要添加逗号

    # 定义元组
    x = 1, 2, 3
    x = (1, 2, 3)
}


将列表转化为元组   
   print tuple([1, 2, 3])

基本操作 {
    索引
        x = 0, 1, 2, 3
        print x[0]
        print x[1:]

    index(object)
    count(object)

    没有添加，删除，排序，反向排列等等方法
}



