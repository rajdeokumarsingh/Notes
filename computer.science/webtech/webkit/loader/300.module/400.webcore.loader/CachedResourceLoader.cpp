provide interface to request image/script/css

has a memory cache to store resource in memory

control auto load image
    when setting of auto load image is enabled, load all images belongs to the document

loading security check
    // Some types of resources can be loaded only from the same origin.  Other
    // types of resources, like Images, Scripts, and CSS, can be loaded from any URL.
load policy
    when to use, reload, revalidate
 
preloading resources


