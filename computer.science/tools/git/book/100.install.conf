Intallation
    Linux
        sudo apt-get install git-core git-doc

        git --version

    Mac OS
        sudo port install git-core +svn +doc
        sudo port install git-core +svn +doc +bash_completion +gitweb

Configuration
    git config
        --global
            user.name "Ray Jiang"
            user.email "jiangrui_tr2008@126.com"

            color.ui (auto | always)
            --list


